/**
 * @autarq/core — Business logic and domain models
 *
 * Rules:
 * - No direct DB access — use repository interfaces
 * - No direct connector instantiation — depend on IConnector interfaces
 * - All behaviour covered by specs/features/ scenarios
 */

export {}
